<nav class="navbar navbar-inverse">
    <div class="container-fluid">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse"
                    data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>

        </div>
        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">

            <form class="navbar-form navbar-left form-inline " id="query-time">
                <div class="form-group">
                    <input type="text" class="form-control" id="m_month" name="month" value="">
                </div>
                <button type="submit" class="btn btn-success"> 查询</button>
            </form>
            <ul class="nav navbar-nav navbar-left">
                <li><a href="{{ path_for('month') }}" style="color:{% if not(active is defined) or active != 'year' %}#f08327{% endif %}">月记录</a></li>
                <li><a href="{{ path_for('year') }}" style="color:{% if active == 'year' %}#f08327{% endif %}">年记录</a></li>
            </ul>
        </div>
    </div>
</nav>